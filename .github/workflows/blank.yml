name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
    
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Cache
      uses: actions/cache@v2.0.0
      with:
        path: /test
        key: 123

